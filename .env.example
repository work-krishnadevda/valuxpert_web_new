# MyValue (ValuXpert) — environment variables
# No environment variables are required to run the site locally.
# This file is provided for future integrations (e.g. a real demo-request
# API endpoint or analytics key) so the project structure stays ready for them.

# VITE_CONTACT_API_URL=https://api.valuxpert.in/demo-requests
# VITE_ANALYTICS_ID=
